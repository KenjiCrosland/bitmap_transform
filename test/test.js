'use strict';
var fs = require('fs');
var expect = require('chai').expect;
var convertBitmap = require(__dirname + '/../bitmap.js');

describe('the bitmap processing function', function(){
  it('should open a bitmap using fs and read it into a buffer', function() {

  });

  it('should convert the buffer into a JavaScript Object', function() {

  });

  it('should invert the colors in the bitmap', function() {

  });

  it('should turn the transformed object back into a buffer', function(){

  });

  it ('should write that buffer to a new file', function(){

  });
});
